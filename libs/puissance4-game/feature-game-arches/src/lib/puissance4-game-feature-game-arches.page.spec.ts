import { puissance4GameFeatureGameArches } from './puissance4-game-feature-game-arches.page';

describe('puissance4GameFeatureGameArches', () => {
  it('should work', () => {
    expect(puissance4GameFeatureGameArches()).toEqual(
      'puissance4-game-feature-game-arches'
    );
  });
});
